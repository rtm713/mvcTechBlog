
{{#each posts as |post| }}
<a href="/post/{{post.id}}" class="blogPost">
    <h2>{{post.title}}</h2>
    <p>{{post.body}}</p>
    <footer>Post created by {{post.user.username}} on {{post.date_created}}</footer>
</a>
{{/each}}